extend layout 

block content 
    h1=title 

    p Are you sure you want to delete this item: #{item.name}?

    form(method="POST" action="")
            div.form-group
                input#itemid.form-control(type='hidden',name='itemid', required='true', value=item._id )
            button.btn.btn-primary(type='submit') Delete