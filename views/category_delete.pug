extend layout 

block content 
    h1=title 

    if items.length 
        p You have items under this category. Please delete them before deleting the category.
        for item in items 
            li 
                a(href=item.url) #{item.name}
    else 
        p Are you sure you want to delete this category?
        form(method="POST" action='')
            div.form-group
                input(type="hidden")
            button.btn.btn-primary(type="submit") Delete